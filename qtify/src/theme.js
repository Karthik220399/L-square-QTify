import { createTheme } from '@mui/material/styles';

let theme = createTheme({
    typography: {
        fontFamily: "Poppins"
    },
    palette: {
        primary: {
         main: "#121212"
        }
    }
})

export default theme;